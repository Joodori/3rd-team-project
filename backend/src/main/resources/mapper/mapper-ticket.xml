<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lx.edu.levelup.feature.ticket.TicketMapper">

	<insert id="insertDB" parameterType="lx.edu.levelup.feature.ticket.TicketDTO">
		insert into ticket(user_no, ticket_name, ticket_amount, ticket_money_status, ticket_reserve_date)
		values(#{userNo}, #{ticketName}, #{ticketAmount}, #{ticketMoneyStatus}, now())
	</insert>
	
	<select id="getTicketList" parameterType="int" resultType="lx.edu.levelup.feature.ticket.TicketDTO">
		select *
		from ticket
		where user_no = #{user_no}
	</select>


</mapper>